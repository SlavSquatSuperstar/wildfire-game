package wildfire.actors.tiles;

import greenfoot.Actor;
import wildfire.Assets;

/**
 * A indicator for a valid build space next to a tile.
 */
public class PreviewTile extends Actor {
    public PreviewTile() {
        setImage(Assets.PREVIEW_TILE);
    }
}
